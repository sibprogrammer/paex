{
  "name": "paex",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^3.1.0"
  },
  "dependencies": {
    "body-parser": "^1.16.0",
    "express": "^4.14.0",
    "plesk-api-client": "^1.0.1"
  },
  "scripts": {
    "start": "concurrently 'npm run server' 'npm run client'",
    "server": "node server.js",
    "client": "node start-client.js"
  }
}
